spring:
  application:
    name: spring-gift
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  mvc:
    hidden method:
      filter:
        enabled: true
  jwt:
    secret: asdfasdf
    access_expiration: 3600000
    refresh_expiration: 604800000